RewriteEngine On
 
# Rewrite /page to /page.html if it exists
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^/]+)/?$ $1.html [L]
 
# Handle subfolder-style URLs too
RewriteCond %{REQUEST_FILENAME}/index.html -f
RewriteRule ^([^/]+)/?$ $1/index.html [L]